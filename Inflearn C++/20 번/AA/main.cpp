#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
using namespace std;

int main(int argc, char** argv) {
	int n; cin>> n;
	int a[n],b[n];
	for (int index =0 ; index < n ; index++) scanf("%d",&a[index]);
	for (int index =0 ; index < n ; index++){
	scanf("%d",&b[index]);
	
	if( a[index])
	
	}
	
	
	return 0;
}
